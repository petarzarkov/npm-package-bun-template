# Bun configuration
# https://bun.sh/docs/runtime/bunfig

[test]
# Only run tests in src/ directory by default
# Use `bun run test:e2e` for e2e tests with proper setup
root = "./src"

# Coverage
coverageDir = "coverage" 
coverageReporter = ["text", "lcov"]

